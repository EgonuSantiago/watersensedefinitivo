name: watersense
description: A new Flutter project.

publish_to: 'none' # Remove this line if publishing to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^1.1.1          # OK
  flutter_blue_plus: 1.31.15  # versão v2 embedding
  shared_preferences: ^2.4.18
  permission_handler: ^12.0.1
  intl: ^0.20.2
  percent_indicator: ^4.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/widgets/launch_image.png
